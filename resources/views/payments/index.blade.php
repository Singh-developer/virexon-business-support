@extends('layouts.app')@section('content')<div class="page-head"><div><div class="eyebrow">PAYMENT MANAGEMENT</div><h1>Payments</h1><p>Gateway-aware payment operations with status, verification and auditability.</p></div><a class="btn primary" href="{{ route('payments.create') }}">+ New Payment</a></div><div class="panel"><form class="toolbar"><input name="q" value="{{ request('q') }}" placeholder="Payment reference or gateway ID"><select name="status"><option value="">All statuses</option>@foreach(['created','pending','processing','successful','failed','cancelled','refunded'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select><select name="gateway"><option value="">All gateways</option><option value="mock">Mock</option><option value="razorpay">Razorpay</option><option value="paytm">Paytm</option></select><button class="btn secondary">Filter</button></form><div class="table-wrap"><table><thead><tr><th>Payment</th><th>Business</th><th>Card</th><th>Amount</th><th>Gateway</th><th>Status</th><th>Created</th></tr></thead><tbody>@foreach($payments as $p)<tr><td><a class="link mono" href="{{ route('payments.show',$p) }}">{{ $p->reference }}</a></td><td>{{ $p->business->name }}</td><td>{{ $p->card?->reference ?? '—' }}</td><td>₹{{ number_format($p->amount,2) }}</td><td>{{ strtoupper($p->gateway) }}</td><td><span class="badge {{ $p->status->value }}">{{ ucfirst($p->status->value) }}</span></td><td>{{ $p->created_at->format('d M Y H:i') }}</td></tr>@endforeach</tbody></table></div><div class="pagination">{{ $payments->links() }}</div></div>@endsection
