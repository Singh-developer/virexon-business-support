@extends('layouts.app')@section('content')<div class="page-head"><div><div class="eyebrow">CLIENT MANAGEMENT</div><h1>Businesses</h1><p>Manage assigned businesses, compliance and payment history.</p></div><a class="btn primary" href="{{ route('businesses.create') }}">+ New Business</a></div><div class="panel"><form class="toolbar"><input name="q" value="{{ request('q') }}" placeholder="Search businesses..."><button class="btn secondary">Search</button></form><div class="table-wrap"><table><thead><tr><th>Business</th><th>Contact</th><th>Users</th><th>Cards</th><th>Payments</th><th>Status</th><th></th></tr></thead><tbody>@foreach($businesses as $b)<tr><td><a class="link" href="{{ route('businesses.show',$b) }}"><strong>{{ $b->name }}</strong></a><small>{{ $b->email }}</small></td><td>{{ $b->contact_person }}<br><small>{{ $b->phone }}</small></td><td>{{ $b->users_count }}</td><td>{{ $b->cards_count }}</td><td>{{ $b->payments_count }}</td><td><span class="badge {{ $b->status==='active'?'success':'neutral' }}">{{ ucfirst($b->status) }}</span></td><td><a class="btn tiny" href="{{ route('businesses.edit',$b) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="pagination">{{ $businesses->links() }}</div></div>@endsection
