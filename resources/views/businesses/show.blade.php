@extends('layouts.app')@section('content')<div class="page-head"><div><div class="eyebrow">BUSINESS PROFILE</div><h1>{{ $business->name }}</h1><p>{{ $business->contact_person }} · {{ $business->email }}</p></div><a class="btn secondary" href="{{ route('businesses.edit',$business) }}">Edit Business</a></div><div class="stats-grid three"><div class="stat-card"><div class="stat-label">Status</div><div class="stat-value small">{{ ucfirst($business->status) }}</div></div><div class="stat-card"><div class="stat-label">Cards</div><div class="stat-value">{{ $business->cards->count() }}</div></div><div class="stat-card"><div class="stat-label">Recent Payments</div><div class="stat-value">{{ $business->payments->count() }}</div></div></div><div class="section-grid"><div class="panel"><div class="panel-head"><h3>Virtual Cards</h3><a href="{{ route('cards.create') }}">Create card →</a></div><div class="list">@forelse($business->cards as $c)<div class="list-row"><div><strong>{{ $c->reference }}</strong><small>{{ $c->cardholder_name }}</small></div><span class="badge {{ $c->status->value==='active'?'success':'neutral' }}">{{ ucfirst($c->status->value) }}</span><strong>₹{{ number_format($c->remaining_limit,2) }} left</strong></div>@empty<div class="empty">No cards.</div>@endforelse</div></div><div class="panel"><div class="panel-head"><h3>Business Details</h3></div><dl class="details"><dt>Phone</dt><dd>{{ $business->phone ?: '—' }}</dd><dt>Address</dt><dd>{{ $business->address ?: '—' }}</dd><dt>GST / Tax</dt><dd>{{ $business->tax_number ?: '—' }}</dd><dt>Created</dt><dd>{{ $business->created_at->format('d M Y') }}</dd></dl></div></div>@endsection
